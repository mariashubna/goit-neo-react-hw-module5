import { NavLink } from "react-router-dom";

const Navigation = () => {
  return (
    <nav>
      <NavLink to="/"></NavLink>
      <NavLink to="/movies"></NavLink>
    </nav>
  );
};

export default Navigation;
